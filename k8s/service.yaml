apiVersion: v1
kind: Service
metadata:
  name: recommendation-api
  namespace: recommendation
  labels:
    app: recommendation-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: recommendation-api
  sessionAffinity: None
