# NOTE: In production, use sealed-secrets or external secret management
# This is a template - replace with actual base64-encoded values
apiVersion: v1
kind: Secret
metadata:
  name: recommendation-secrets
  namespace: recommendation
type: Opaque
data:
  # Base64 encoded values (example: echo -n 'password' | base64)
  DATABASE_USER: dXNlcg==  # 'user'
  DATABASE_PASSWORD: cGFzc3dvcmQ=  # 'password'
  REDIS_PASSWORD: ""  # empty if no password
